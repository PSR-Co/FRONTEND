import 'package:flutter/material.dart';

// TODO: 자주 사용되는 레이아웃 정의
class DefaultLayout extends StatelessWidget {
  const DefaultLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
